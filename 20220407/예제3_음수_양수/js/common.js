'use strict'
// 변수 선언

let num = prompt('숫자를 적어주세요');
let numX = Number(num);

const numCheck = 0;

if (numX < numCheck) {
  alert(`입력하신 숫자 ${numX}은 음수입니다.`)
}if (numX > numCheck) {
  alert(`입력하신 숫자 ${numX}은 음수입니다.`)
}else if (numX == numCheck) {
  alert(`입력하신 숫자 ${numX}은 0입니다.`)
}
